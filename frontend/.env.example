# Backend URL used by the browser (must be reachable from the user's machine).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Backend URL used by Next.js server (SSR / route handlers). Inside Docker compose this
# is typically the service name, e.g. http://backend:8080.
BACKEND_URL=http://localhost:8080
